export default function PostTitle({ children }) {
  return (
    <h1 className="mb-12 font-poppins text-center text-7xl font-semibold leading-tight tracking-tighter md:leading-none">
      {children}
    </h1>
  )
}
